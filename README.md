# ml-learning

From-scratch implementations while working through Cornell CS4780 (Kilian Weinberger).

Raw NumPy only — no scikit-learn classifiers, no PyTorch. scikit-learn is used
for loading datasets, never for the algorithms themselves.

| File | Topic |
|---|---|
| `01_knn.ipynb` | k-nearest neighbours, Euclidean distance, train/test split |
